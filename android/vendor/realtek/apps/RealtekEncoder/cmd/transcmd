# Script to start "pm" on the device, which has a very rudimentary
# shell.
#
base=/system
export CLASSPATH=$base/framework/transcode.jar
exec app_process $base/bin com.realtek.commands.transcode.Transcode "$@"

